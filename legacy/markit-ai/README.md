# markit-ai

This package moved to **[@shift-labs/markit](https://www.npmjs.com/package/@shift-labs/markit)**.

`markit-ai@0.6.0` is a compatibility shim. Existing imports and the `markit`
CLI continue to work, but new installations should use:

```bash
npm install @shift-labs/markit
```
